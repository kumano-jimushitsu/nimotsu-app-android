package com.example.top;

public enum enum_event {
    uid,
    created_at,
    event_type,
    parcel_uid,
    ryosei_uid,
    room_name,
    ryosei_name,
    target_event_uid,
    note,
    is_finished,
    is_deketed,
    sharing_status
}
